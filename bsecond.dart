void main() {
  //In dart all type is object and thats why wwe can assign the null on any object
  int? num = null; // Here if we have a situatuon like we want to define a variable which will either cantian a value or also a null then we add a question mark at the end of datatype
  
  num = 10;
  print(num);
}